# Main source directory configuration

# Add component subdirectories
add_subdirectory(storage)

# Future components
# add_subdirectory(network)
# add_subdirectory(replication)
# add_subdirectory(data_structures)
